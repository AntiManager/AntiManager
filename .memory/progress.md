# AntiManager — Progress
> Updated: 2026-09-21

## Done
- [x] Brutalist rebuild v2: Black+Red+White, 32 pages, star map (static SVG)
- [x] SEO: OG, canonical, structured data, favicon, sitemap
- [x] UX fixes: A11y, hamburger menu, scroll-top, mobile grids, hover guards
- [x] Cache-busting `?v=`, nginx hardening, permission fix
- [x] Playwright tests: 84 (28 × 3 viewports)
- [x] Secrets extracted to .env, sensitive data scrubbed
- [x] Repo restructured: site/ + manifestos root
- [x] Harness audit & optimization cascade (C1–C5): docs/config repaired, skills rewritten to fact, portable config, harness map, E2E made hermetic (66/66). Log + retrospective in `.kilo/plans/1790002457744-*`.
- [x] Follow-up QA: AX08 mobile-nav, AX09 arsenal zone-filter, AX10 system-map presence added; suite 84/84. Committed + pushed.
- [x] Security hardening (audit 2026-09-21): output escaping in `build.js` (text fields, slug/zone/status, JSON-LD), DOM-XSS fixed in live widgets 04/16/19 (+AX11 regression, falsifiability-checked), dead JS removed (4 files / 682 lines + `dist` clean), nginx security headers, broken OG image fixed, username leak in `add-fm-00.py` fixed. Suite 93/93. Log: `.kilo/plans/1790006200000-*`.
- [x] Security hardening follow-up: closed review findings — extracted `site/src/lib/escape.js` (+`assertSlug` slug/id validation, closes output-dir traversal), weapon zone badge via `zoneBadgeHtml()`, canonical URL escaped; added automated regression tests `site/unit/escape.test.js` + `site/unit/build-escape.test.js` (`WEAPONS_FILE` seam, falsifiability-checked); npm `test:unit`/`test:e2e`. Unit 4/4, suite 93/93. Committed `789755c`, pushed to GitHub + GitVerse, deployed to `antimanager.pro` (verified 200 + security headers).
- [x] SEO & content hardening — Phase 1 (technical): dynamic `BUILD_DATE` → sitemap `lastmod` + JSON-LD dates; landing featured 4 cards (was 1); related-links fallback for 6 zone-less weapons; `article:modified_time`/`article:tag`, `og:image` dims, static `BreadcrumbList`; landing meta fixed («4 контура, 22 главы»); raster `og-image.png` (`site/scripts/render-og.js`, `npm run render:og`); tests `site/unit/build-seo.test.js` + `AX12`, `test:unit` serialized. Unit 5/5, suite 96/96. Log: `.kilo/plans/1790008000000-*`. Phase 2 (statuses/stats) pending decision.
- [x] Content readiness audit + harness skill: scanned 30 vault articles (12-point rubric). 22 site articles structurally READY; 23 NEEDS-EDIT; appendices 30–35 DRAFT. Added skill `article-readiness`, command `/article-readiness`, script `.kilo/scripts/article_readiness.py` (report in gitignored `.kilo/reports/`); wired into `book-writing` + `book-editor`. Publication blocked by frontmatter `review`/`book/draft`, not by content.
- [x] SEO review fixes: `plural()` helper in `build.js` (landing meta), dropped duplicate `.slice(0,4)`, guarded `og:image:width/height` for `opts.ogImage` override, `render-og.js` temp-file leak fixed, brittle sitemap assertion dropped. `VERSION`/`BUILD_DATE` coupling deferred to next release. Unit 5/5, suite 96/96.
- [x] Harness cleanup: `/check-site` now runs the full suite (unit + e2e, expected 5 + 96); removed stale `.gigacode/` from `vault-indexer`; compacted `active_context` 6.5 KB → 2.0 KB (history → `.memory/archive/active_context-2026-09.md`). Committed + pushed.
- [x] Publish-readiness hardening, article-by-article (Layer 2). Article **00 «Маятник управления»** brought to 12/12: fixed 9 factual/attribution errors (Lepanto **1571** + outcome framing; Gastev executed **1939**; misattributed **Jevons** → generic fatigue research; Amazon AI **2014–2017**, not 2024; Hammurabi wage units → gur; Luddites 1810s; Owen 1800), removed folklore numbers (Arsenal «2 hours», «galley a day»), replaced unsourced 76% stress stat, SMART attribution hedged; populated «Связанные инструменты» ([[Цикл PDCA]]/[[Канбан]]/[[Agile]]/[[12 рычагов Медоуз]]), +2 related chapters, labeled the Wells Fargo case; `updated` bumped. Encoding OK (guard). **Site synced** — `site/src/content/00-mayatnik-upravleniya.html` updated: table facts (Venice/Taylor + new Gastev row), widget re-aligned to the source's 4 признака формализации + static «5 принципов деформализации». Build OK, suite 96/96; widget verified in-browser (both result branches, back button).
- [x] Publish-readiness hardening, article **01 «Manifestum Imperii Rationalis»** → **12/12 READY** (was 7/12 DRAFT). Vault (canonical): valid frontmatter (`tags/status/created/updated/aliases`), title H2 + subtitle, `## Преамбула / 10 ценностей / 15 принципов / Заключение` structure, tabs→spaces, typos («чего то»→«чего-то», «рожденный»→«рождённый»), +«Связанные инструменты» ([[Цикл PDCA]]/[[Канбан]]/[[12 рычагов Медоуз]]/[[Pre-Mortem]]) и +«Связанные главы» (02/11/14/17). Note: Layer 1 `case`/`conclusion` previously passed only as false positives («вы**водим**», «**практик**и»). **Site:** `/manifesto/` was hardcoded in `build.js` with only 10 value titles (no preamble/principles/closing) despite «ЧИТАТЬ ПОЛНОСТЬЮ»; expanded to the full document via structured `manifestoValues/manifestoPrinciples/manifestoClosing` (also drives the landing teaser — no drift), +CSS `.manifesto-preamble`/`.manifesto-value-body`, +`AX13` regression. Root `manifesto-ru.md` synced to the vault canon (added missing «К нам присоединяются…» paragraph, ё fixes). Encoding OK (guard). Unit 5/5, suite 99/99.
- [x] Deployed articles 00/01 to production: committed `2910047` (site content + full `/manifesto/` + AX13), `deploy.ps1` built 22 articles, copied `dist`, restarted `antimanager-web`. Caddy-reload step hit a transient SSH timeout but is optional (Caddy config unchanged). Verified live: `/manifesto/` full document (preamble + 10 values + 15 principles + closing), `/weapons/mayatnik-upravleniya/` Gastev row + five-rules block + old false fact gone, landing CTA. Note: `Invoke-WebRequest` decodes responses as Latin-1 (no charset) — verify by downloading bytes and reading as UTF-8.
- [x] Publish-readiness hardening, article **02 «Управление сложными системами»** → **12/12** (was already 12/12 structurally). Layer 2 editorial pass on the vault canonical (not a git repo — remotely-save): repaired the lost code fences in «Шаблон для первого шага» (Obsidian had leaked UI labels `text`/`Копировать`/`Скачать` as body text) → proper fenced block; fixed broken internal wiki links `[[02_…#3.4 Пирамида Хапрова|…]]` ×2 (no such heading) → existing tool note `[[7 уровней сложности Хапрова]]`; tabs→spaces ×4; typography normalised to the 00/01 standard — parenthetical `–`→`—` ×114 and numeric ranges `5-6`/`1950-60-е`→en dash; +related chapters 11/17 (now 6); `updated` bumped. Encoding OK (guard). **CORRECTION:** that pass was cosmetic only — the 12/12 was structural + formatting, no substantive editorial and no site sync; this record overstated it. **Full cycle completed (this session):** vault Layer 2 (typo «кросфункциональных»→«кросс-функциональных»; removed two leftover fully-italic table rows; «...»→«…»; hedged the folklore «120 тысяч лет» claim; framed the food-plant anecdote as author experience; Bogdanov → «1910–20-е»; «Икудзиро Нонака»; +tools [[Канбан]]/[[Сценарное планирование]] → 4; readiness 12/12, wiki 12, guard OK before/after). Site: landing 02 «Тезисный отрывок» → `{{read_more}}`; authored `src/content/full/02-slozhnye-sistemy.html`; `/full/` built (static TOC 31 anchors, `.materials-section` + `materials/slozhnye-sistemy.pdf` 222 KB, 4-level BreadcrumbList, sitemap). Unit 8/8 (+rollout regression), Playwright 99/99. Committed/pushed/deployed.
- [x] Publish-readiness hardening, article **03 «Системная динамика в производстве»** → **12/12**. Layer 2 editorial (vault canonical, remotely-save): fixed 2 broken wiki links — `[[CLD (Causal Loop Diagram)]]` → the real note `[[Диаграмма причинно-следственных связей (Causal Loop Diagram - CLD)|CLD]]`, and `[[Системное обучение]]` (no such note) → `[[Цикл PDCA (Deming Cycle)|Цикл PDCA]]`; «КПИ»→«KPI»; non-breaking hyphen U+2011 → regular; +related tools `[[12 рычагов Медоуз]]`, `[[Метод «5 почему» (Five Whys)]]` (now 3); +related chapters 11/17/20 (now 7); `updated` bumped. Typography already standard (107 em dashes; 2 en dashes only in ranges). Encoding OK (guard). **CORRECTION:** that pass was formatting/structure only — no site sync; full cycle completed 2026-09-21 (below).
- [x] Publish-readiness hardening, article **03 — full cycle completed**. Vault Layer 2 (remotely-save): intro «ты»→«вы» (rest of the book uses «вы»); dropped unsourced 90%/5% figures in the training table → qualitative; added 8-archetype source attribution (Senge «Пятая дисциплина», Meadows «Думая о системах»); +`[[Цикл PDCA (Deming Cycle)|Цикл PDCA]]` to related tools. Readiness 12/12, wiki 13, guard OK (3.5% uppercase). Site: landing «Тезисный отрывок» → `{{read_more}}`, principle list aligned to the 8 article names, «Успех к неуспеху»→«Успех победителя» (table + widget); authored `src/content/full/03-sistemnaya-dinamika.html`; `/full/` built (24 TOC anchors, `.materials-section` + `materials/sistemnaya-dinamika.pdf` 379 KB, 4-level BreadcrumbList, sitemap); new `.full-part-lede` CSS so part subtitles don't become the auto read-more lede. Unit 8/8 (rollout regression generalized to 02+03), Playwright 99/99. Committed `d1d82ce`, pushed (GitHub+GitVerse), deployed; live-verified: landing `.read-more-card` → `/full/` (no «Тезисный отрывок»/`download-section`), `/full/` 200 (TOC, materials, PDF link, 4-level breadcrumb), `/materials/sistemnaya-dinamika.pdf` 200 379 170 B.
- [x] Publish-readiness hardening, article **04 «Управление как эксперимент» — full cycle**. Vault Layer 2 (remotely-save): fixed PDCA/PDSA attribution in the intro + appendix historical note (Shewhart's idea 1939 → Deming's four-step, Deming preferred Study over Check); corrected an internal arithmetic contradiction in the casting case (12%→6% on 2M losses cannot save 1.8M → «примерно с 2 млн до 1 млн руб. в месяц»); normalised straight quotes («риски», «Как собирать требования»); fixed the tab-broken numbered list in the hypothesis-map example; +`[[Метод «5 почему» (Five Whys)]]` to tools and +`[[21_Два_типа_управления|21. Два типа управления]]` to chapters; `updated` bumped. Readiness 12/12, wiki 8, guard OK (2.9% uppercase). Site: landing «Тезисный отрывок» → `{{read_more}}`; authored `src/content/full/04-upravlenie-eksperiment.html` (13 H2 / 34 H3); `/full/` built (45 TOC links, `.materials-section` + `materials/upravlenie-eksperiment.pdf` 488 KB, 4-level BreadcrumbList, sitemap); rollout regression extended to 04. Unit 8/8, Playwright 99/99. Committed `277cf8b`, pushed (GitHub+GitVerse), deployed; live-verified: landing `.read-more-card` → `/full/` (no «Тезисный отрывок»/`download-section`), `/full/` 200 (TOC 45, materials, PDF link, `"position": 4`, no tokens), `/materials/upravlenie-eksperiment.pdf` 200 `application/pdf` 488 542 B. Caddy-reload ssh step timed out again (config unchanged, no-op).

- [x] **Two-tier theme pages** (funnel: landing → full text → materials). Removed the fake site-wide `download-section` («Готовим PDF… Хочу PDF», no backend) and the dead mid-page «Тезисный отрывок». Build now: `/weapons/<slug>/` (interactive) + `/weapons/<slug>/full/` (full text) with a `{{read_more}}` placeholder → `.read-more-card` (lede + read time + CTA) only when `src/content/full/XX-slug.html` exists. Full page: static H2/H3 TOC, reading-progress bar, 4-level BreadcrumbList, article `Article` schema, sitemap entry. New `materials.json` (validated `href`), `materials/<slug>.pdf` auto-detected → `.materials-section` (honest: article PDF + declared extras, print fallback when absent), `@media print` strips site chrome; `scripts/render-pdf.js` + `npm run render:pdf` (Playwright). **Pilot article 00** converted from vault (464 lines) → `src/content/full/00-mayatnik-upravleniya.html`, landing excerpt → `{{read_more}}`; PDF 423 KB. Tests: unit 7/7, e2e 99/99 (AX06 rewritten). Log: `.kilo/plans/1790011926918-full-article-pages-execution-log.md`. Committed `0e4e511`, pushed (GitHub+GitVerse), deployed; live-verified: landing read-more card (no «Тезисный отрывок»/`download-section`/`download-btn`), `/full/` 200 (TOC, materials, PDF link, 4-level `BreadcrumbList`), `/materials/mayatnik-upravleniya.pdf` 200 (423 713 B). Optional Caddy-reload step hit a transient SSH timeout (Caddy config unchanged).

## Planned
- Roll `/full/` out article-by-article as vault readiness allows (00 pilot, 02, 03, 04 done; 01 has no weapon entry — its full text is `/manifesto/`).
- Then article **05 «Каскадирование целей»** — Layer 2 editorial pass + site sync.
- Publish per-article extras (checklists/templates) into `site/materials/<slug>/` + `materials.json`.
- SEO plan Phase 2 (decision): publish 21 `review` weapons; replace fabricated landing stats.

## Technical Debt
- **GitVerse token** embedded in `.git/config` `origin.pushurl` (plaintext credential) — rotate + move to credential helper/SSH. Not a repo file.
- **F16.3 (diagnosed, closed as upstream/cosmetic)** — `config_validation ERROR: Failed to parse frontmatter: No context found for instance` on writes under `.kilo/command|agent/**` is an **upstream Kilo 7.7.5 bug**, content-independent: the write-path validation helper reads the `instance` `LocalContext` (AsyncLocalStorage, `Jo`) outside the scope that provides it, so `GfD()` throws `NotFound("instance")`. The write still succeeds; commands/agents load normally. Secondary upstream defect: `trusted` uses `path.isAbsolute(P) && path.posix.isAbsolute(P)` → `false` for Windows paths. No local fix.
- Metrika `webvisor:true` vs `/privacy/` claim — deferred by user (compliance risk remains).
